% TikZ: Clinical data pipeline (document to temporal DB)
\begin{tikzpicture}[
  node distance=0.5cm and 0.8cm,
  box/.style={draw, rounded corners, fill=blue!8, minimum height=0.6cm},
  arrow/.style={->, thick}
  ]
  \node[box] (pdf) {PDF / Image};
  \node[box, right=of pdf] (ocr) {OCR (Tesseract / EasyOCR)};
  \node[box, right=of ocr] (parse) {Prescription \& Lab Parsers};
  \node[box, right=of parse] (temporal) {Temporal PDF Parser};
  \node[box, below=0.8cm of temporal] (db) {Clinical Patient DB\\Labs, Vitals, Events};
  \draw[arrow] (pdf) -- (ocr);
  \draw[arrow] (ocr) -- (parse);
  \draw[arrow] (parse) -- (temporal);
  \draw[arrow] (temporal) -- (db);
\end{tikzpicture}
